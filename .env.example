# Bridge Server Configuration
PORT=3000
NODE_ENV=development

# Knowledge-Agent-POC Backend
KNOWLEDGE_API_URL=http://localhost:8000
KNOWLEDGE_API_TIMEOUT=30000

# Authentication
JWT_SECRET=your-jwt-secret-key-change-in-production
JWT_ISSUER=https://eventhub.internal.microsoft.com
JWT_AUDIENCE=event-hub-apps

# CORS - Allowed Origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174,https://rrs25demoapp.azurewebsites.net

# Logging
LOG_LEVEL=info

# ========================================
# Azure Key Vault - Customer-Managed Keys (CMK)
# ========================================
# Enable CMK for encryption at rest
# Set to 'true' to enable Key Vault integration
CMK_ENABLED=false

# Key Vault URI (from deployment outputs)
# Example: https://kv-event-hub-bridge-dev-xxx.vault.azure.net/
KEY_VAULT_URL=

# CMK key name in Key Vault
ENCRYPTION_KEY_NAME=event-hub-cmk

# Optional: Specific key version (if not provided, uses latest)
ENCRYPTION_KEY_VERSION=

# Azure Authentication for Key Vault
# If using Managed Identity (recommended in Azure), leave these empty
# Otherwise, provide credentials:
