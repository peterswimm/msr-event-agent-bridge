# ========================================
# BRIDGE SERVER - NON-SENSITIVE CONFIG
# ========================================
PORT=3000
NODE_ENV=production
LOG_LEVEL=info

# Backend API Configuration
KNOWLEDGE_API_URL=https://backend.eventhub.internal/api
KNOWLEDGE_API_TIMEOUT=30000

# Azure Entra ID Authentication Configuration
AZURE_TENANT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
AZURE_CLIENT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
JWT_AUDIENCE=api://event-hub-bridge

# JWT Configuration (legacy - use Azure Entra ID instead)
JWT_ISSUER=https://eventhub.internal.microsoft.com

# CORS - Allowed Origins (comma-separated)
ALLOWED_ORIGINS=https://eventhub.internal.microsoft.com,https://teams.microsoft.com

# Foundry delegation (feature-gated; defaults off)
# Toggle delegation from bridge to backend/Foundry. Requires backend support.
DELEGATE_TO_FOUNDRY=false
# Optional Entra role required to allow delegation; leave empty to allow all authenticated users.
FOUNDRY_REQUIRED_ROLE=

# ========================================
# AZURE KEY VAULT INTEGRATION
# ========================================
# Key Vault URL where secrets are stored
# Example: https://kv-event-hub-bridge-prod.vault.azure.net/
KEY_VAULT_URL=https://kv-event-hub-bridge-prod.vault.azure.net/

# Azure Tenant ID for authentication
AZURE_TENANT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# ========================================
# SECRETS (STORED IN KEY VAULT, NOT HERE!)
# ========================================
# The following secrets must be created in Azure Key Vault:
# 
# jwt-signing-key
#   - JWT token signing key for authentication
#   - Rotation: 30 days
#
# openai-api-key
#   - Azure OpenAI API key
#   - Rotation: As needed
#
# database-connection-string
#   - PostgreSQL connection string with password
#   - Rotation: 90 days
#
# redis-password
#   - Redis authentication password
#   - Rotation: As needed
#
# DO NOT ADD ANY SECRETS TO THIS FILE!
